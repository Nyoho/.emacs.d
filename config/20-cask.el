;;
;; Cask
;; Before using cask.el, do this:
;; curl -fsSkL https://raw.github.com/cask/cask/master/go | python
;;
;; (require 'cask "~/.cask/cask.el")
;; (cask-initialize)

;; (use-package cask
;;   :defer t
;;   :config
;;   (require 'cask "~/.cask/cask.el")
;;   :init
;;   (cask-initialize))

;; (use-package pallet
;;   :defer t
;;   :init
;;   (pallet-mode t))
