;; -*- lexical-binding: t; -*-
(when window-system
  ;; (add-to-list 'default-frame-alist '(alpha . (100 83)))

  ;; (add-to-list 'default-frame-alist '(foreground-color . "VioletRed1"))
  ;; (add-to-list 'default-frame-alist '(background-color . "black"))
  ;; (add-to-list 'default-frame-alist '(cursor-color . "orange red"))
  ;; (add-to-list 'default-frame-alist '(mouse-color . "light slate blue"))
  )
